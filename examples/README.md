# Examples

Run the scanner against this folder:

```bash
PYTHONPATH=../src python3 -m vibeauditor . --no-color
```

The TypeScript example intentionally contains an unauthenticated database write
and user input flowing into prompt construction.
